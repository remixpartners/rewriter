.DS_Store
*.pyc
__pycache__/
.env
*.skill

# Voice guides contain personal communication patterns -- keep out of public repos
# Remove these lines if you want to version-control voice guides
# (both forms matter: `voices/` matches a real directory, `voices` matches a symlink)
voices/
voices
